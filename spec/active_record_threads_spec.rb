require 'spec_helper'

describe ActiveRecordThreads do
  it 'has a version number' do
    expect(ActiveRecordThreads::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
